@{
<<<<<<< HEAD
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
=======
    ViewData["Title"] = "Home";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    body {
        background-color: #f0f7f3;
    }

    .home-wrapper {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        min-height: 90vh;
        padding: 2rem;
        text-align: center;
    }

    .welcome-box {
        background-color: #ffffff;
        border-radius: 16px;
        box-shadow: 0 8px 20px rgba(0, 80, 40, 0.08);
        padding: 3rem 2rem;
        max-width: 600px;
        width: 100%;
    }

        .welcome-box h1 {
            color: #2e7d32;
            margin-bottom: 1rem;
        }

        .welcome-box p {
            color: #5f6f64;
            margin-bottom: 1rem;
        }

    .btn-agri {
        background-color: #4caf50;
        color: white;
        border: none;
        padding: 0.75rem;
        font-size: 1rem;
        border-radius: 8px;
        width: 100%;
        margin-bottom: 1rem;
    }

        .btn-agri:hover {
            background-color: #388e3c;
        }

    .btn-outline-agri {
        border: 2px solid #4caf50;
        color: #4caf50;
        background-color: transparent;
        padding: 0.75rem;
        font-size: 1rem;
        border-radius: 8px;
        width: 100%;
    }

        .btn-outline-agri:hover {
            background-color: #e8f5e9;
            color: #2e7d32;
        }

    header.nav-header {
        background-color: #ffffff;
        box-shadow: 0 2px 6px rgba(0, 64, 32, 0.05);
        margin-top: 1.5rem;
    }

    header .nav-link {
        font-weight: 500;
    }

        header .nav-link:hover {
            text-decoration: underline;
        }
</style>

<div class="home-wrapper">
    <div class="welcome-box">
        <h1>Welcome to</h1>
        <h1>AgriEnergyConnect</h1>
        <p class="lead">Empowering Farmers through Sustainable Energy</p>
        <p>We connect farmers with green energy solutions and streamline agricultural operations through smart digital tools.</p>
        <p>Join us to build a cleaner, smarter future for farming.</p>

        <a asp-controller="Account" asp-action="Login" class="btn btn-agri">Login</a>
        <a asp-controller="Account" asp-action="Register" class="btn btn-outline-agri">Register</a>
    </div>
</div>

@if (User.Identity != null && User.Identity.IsAuthenticated)
{
    <header class="nav-header">
        <div class="container py-3 d-flex justify-content-between align-items-center">
            <h1 class="h4 text-success m-0">AgriEnergyConnect</h1>
            <nav>
                <ul class="nav">
                    @if (User.IsInRole("Farmer"))
                    {
                        <li class="nav-item">
                            <a class="nav-link text-success" asp-controller="Farmer" asp-action="MyProducts">My Products</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-success" asp-controller="Farmer" asp-action="AddProduct">Add Product</a>
                        </li>
                    }
                    @if (User.IsInRole("Employee"))
                    {
                        <li class="nav-item">
                            <a class="nav-link text-success" asp-controller="Employee" asp-action="ViewAllFarmers">View Farmers</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-success" asp-controller="Employee" asp-action="AddFarmer">Add Farmer</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-success" asp-controller="Employee" asp-action="FilterProducts">Filter Products</a>
                        </li>
                    }
                </ul>
            </nav>
        </div>
    </header>
}
>>>>>>> 006fc78 (css)
